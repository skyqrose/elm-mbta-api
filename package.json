{
  "scripts": {
    "build": "elm make",
    "watch": "npm run build; chokidar src/ -c 'npm run build'",
    "format": "elm-format --yes .",
    "test": "elm-test && elm-format --validate . && elm make --docs=docs.json"
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.3",
    "elm-doc-preview": "^3.0.1",
    "elm-format": "^0.8.2",
    "elm-test": "^0.19.0-rev6"
  }
}
