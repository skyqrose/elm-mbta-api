{
  "scripts": {
    "build": "elm make",
    "format": "elm-format --yes .",
    "test": "elm-test && elm-format --validate ."
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps"
  },
  "devDependencies": {
    "elm-format": "^0.8.1",
    "elm-test": "^0.19.0-rev6"
  }
}
